name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ${{ matrix.os }}
    strategy:
      # Phase 4 PR 4.8 — Windows is gating. fail-fast off so a failure on
      # one OS doesn't cancel the others' diagnostics.
      fail-fast: false
      matrix:
        # Pinned versions per phase-4-plan §1: macos-14 + ubuntu-22.04
        # (Phase 1–3 baseline) + windows-2022 (Phase 4 promotion).
        os: [macos-14, ubuntu-22.04, windows-2022]
        node-version: [22]

    steps:
      - uses: actions/checkout@v4

      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Rebuild native modules
        run: npx electron-rebuild -f -w better-sqlite3

      - name: Run tests
        run: npm test

      - name: Check SDK type drift (PR 4.9)
        if: matrix.os == 'ubuntu-latest'
        run: npm run check:sdk-drift

      # Phase 5 PR 5.2 — also cross-reference the published @os8/sdk-types
      # package on npm. continue-on-error: true initially so a desktop PR
      # doesn't have to wait on a fresh npm publish; the warning gets noisy
      # as drift accumulates. Flip to hard-fail 30 days after the v1.0.0
      # publish has been stable.
      - name: Check SDK type drift vs published npm package (PR 5.2)
        if: matrix.os == 'ubuntu-latest'
        continue-on-error: true
        run: npm run check:sdk-drift -- --include-published
